{
  "name": "ledger",
  "private": true,
  "scripts": {
    "test": "node tests/browser.cjs",
    "deploy": "node scripts/stamp-version.mjs && npx wrangler deploy --tag \"$(git rev-parse --short HEAD)\" --message \"$(git log -1 --format=%s)\""
  },
  "devDependencies": {
    "puppeteer": "25.10.0"
  }
}
